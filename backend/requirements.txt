# NOTE: this pulls in the CPU-only build of torch as a dependency of
# ultralytics. If you have an NVIDIA GPU, follow the extra step in the
# top-level README to swap it for the CUDA build -- detection is
# roughly 5x faster on GPU (measured: ~5min -> ~1min for a 20s clip).
fastapi
uvicorn[standard]
python-multipart
ultralytics
opencv-python
pandas
reportlab
requests            # iNaturalist / Gemini HTTP calls
google-genai        # optional: Gemini Vision engine (vision_id.py)
# open_clip_torch   # optional: local CLIP engine (clip_id.py) -- heavy, install only if used
